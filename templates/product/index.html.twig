{% extends 'baseAdmin.html.twig' %}



{% block title %}Product index{% endblock %}

{% block body %}


<h1>Product index</h1>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Ref</th>
            <th>Name</th>
            <th>Description</th>
            <th>Price</th>
            <th>Review</th>
            <th>State</th>
            <th>actions</th>
        </tr>
    </thead>
    <tbody>
        {% for product in products %}
        <tr>
            <td>{{ product.id }}</td>
            <td>{{ product.ref }}</td>
            <td>{{ product.name }}</td>
            <td>{{ product.description }}</td>
            <td>{{ product.price }}</td>
            <td>{{ product.review }}</td>
            <td>{{ product.state }}</td>
            <td>
                <img src="{{ asset('/users/5/'~ product.imgURL) }}"></img>
            <td>
                <div class="flex row">
                    <a href="{{ path('app_product_show', {'id': product.id}) }}" class="btn btn-dark mr-2">show</a>
                    <a href="{{ path('app_product_edit', {'id': product.id}) }}" class="btn btn-success">edit</a>
                </div>
            </td>
        </tr>
        {% else %}
        <tr>
            <td colspan="8">no records found</td>
        </tr>
        {% endfor %}
    </tbody>
</table>

{% endblock %}