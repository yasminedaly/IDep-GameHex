<form method="post" action="{{ path('app_user_delete', {'userID': user.userID}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.userID) }}">
    <button class="btn">Delete</button>
</form>
